import { SliceDomainPipe } from './slice-domain.pipe';

describe('SliceDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
